namespace EP.SOLID.OCP.Solution
{
    public class DebtSavingsAccount : DebitAccount
    {
        public override string Charge(decimal valor, string conta)
        {
            // Validates the Account by Birthday Information
            // Debt Savings Account
            return transaction();
        }
    }
}